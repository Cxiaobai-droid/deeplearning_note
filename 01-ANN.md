# 神经网络
神经网络和深度学习是有交集关系。 
## 神经网络的特征
1.信息表示是分布式的  
2.记忆和知识存储在单元之间的连接上  
3.通过改变连接的强度调整学习的大小  
## 激活函数
1. S型激活函数：sigmoid(logistic,tanh)  
![sigmoid](https://latex.codecogs.com/svg.image?\sigma(x)=\frac{1}{1+e^{-x}})、![tanh](https://latex.codecogs.com/svg.image?\tanh(x)=\frac{e^{x}-e^{-x}}{e^{x}+e^{-x}})  
2. 斜坡型激活函数  
（1）RELU ：  
优点【不会像sigmoid一样饱和、计算简单、一定程度上解决梯度下降的问题】  
缺点【小于0部分可能会永远不被激活、不是0均值，对梯度有影响】  
note:改进措施就是加一些min()或exp()这类的。  
（2）解决RELU死亡现象---->leaky RELU  
（3）解决非0均值---->ELU
3. 复合激活函数  
（1） swish ： 是self-gated函数，其中的logistic函数值域（0，1），logistic值接近于1时值接近x（门开），反之亦然。  
（2） GELU(Gaussian error loss unit)  ： x*P(X<x)，和swish相似。
## 前馈神经网络
1.有向无环图，一个输入层，多个隐藏层，一个输出层。  
隐藏层 ： 可以将m维输入向量仿射变换成n维。每一个隐藏层有不同的目的。（例如：第一层可能识别边缘，第二层组合边缘为形状，第三层识别物体部件。）  
2.前馈神经网络信息仅向前传递，向前传播计算输出，然后反向传播计算梯度从而更新权重，没有反馈步骤，计算简单。  
3.例子 ： 
（1）用于二分类：输入像素m*n，隐藏层用RELU函数转化为更抽象的模式，最后输出层用softmax函数把值转为（0，1）之间的概率分布，分别对应数字0~9.  
（2）用于回归预测： 接受多个特征，用非线性函数（如sigmoid,REL）提取出更复杂的特征，输出预测房价。  
## 反向传播back propagation
1.基本概念  
（1）前向传播forward propagation  
（2）损失函数：通常有MSE和交叉熵损失。  
（3）梯度下降：通过计算损失函数对权重的梯度，进行更新权重，较少损失函数值。  
2.反向传播的目的  
利用链式法则，总输出层向输入层逐步计算误差梯度，高校求解神经网络参数的偏导数，从而实现网络参数的最优化和损失函数最小化。  
3.链式法则  
将复杂复合损失函数转化为简单梯度计算。  
4.偏导数  
求解损失函数随参数变化的敏感度，从而指导参数优化。  
5.例子  
输入层到隐藏层权重W1偏置b1--->隐藏层到输出层权重W2偏置b2  
（1）前向传播  
（2）损失函数MSE=Σ（真实-预测）^2  
（3）反向传播  
* 输出层的“误差责任” ($\delta_2$)  
  * $2 \times (\text{预测} - \text{目标}) \times \text{激活函数的导数}$  
* 隐藏层的“误差责任” ($\delta_1$)  
  * $\delta_1 = (\delta_2 \times W_2) \times \text{隐藏层自己的激活函数导数}$    
* 梯度下降更新参数  
  * $\text{新权重} = \text{旧权重} - \text{学习率} \times \text{梯度}$  
  * 梯度 $=$ 这一层的误差责任 ($\delta$) $\times$ 上一层的输入  
